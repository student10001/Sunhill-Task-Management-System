rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for all authenticated/demo users in this application
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
